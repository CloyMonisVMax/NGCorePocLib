use_frameworks!

platform :ios, '9.0'

target 'NGCorePocLib_Example' do
  pod 'NGCorePocLib', :path => '../'
  pod 'AppCenter'

  target 'NGCorePocLib_Tests' do
    inherit! :search_paths

    
  end
end
